# Custom symlinks configuration
# Format: source_path:target_path
# Source is relative to dotfiles root (~/dotfiles), target is absolute system path
# Lines starting with # are ignored
# Empty lines are ignored

# SDDM theme
sddm/themes/silent:/usr/share/sddm/themes/silent

