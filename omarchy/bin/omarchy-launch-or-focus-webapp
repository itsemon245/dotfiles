#!/usr/bin/env bash

if (($# == 0)); then
  echo "Usage: omarchy-launch-or-focus-webapp [window-pattern] [url-and-flags...]"
  exit 1
fi

WINDOW_PATTERN="$1"
shift
LAUNCH_COMMAND="$HOME/bin/omarchy-launch-webapp $@"

exec "$HOME/bin/omarchy-launch-or-focus" "$WINDOW_PATTERN" "$LAUNCH_COMMAND"
